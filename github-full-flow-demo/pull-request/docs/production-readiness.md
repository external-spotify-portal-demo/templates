# Production readiness

This change was proposed by the Spotify Portal golden path for `${{ values.name }}`.

The pull request keeps production configuration reviewable and auditable before it is merged.
